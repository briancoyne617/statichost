fastapi==0.135.1
starlette==0.52.1
uvicorn==0.42.0
jinja2==3.1.6
python-multipart>=0.0.9  # FastAPI imports this LAZILY when a Form() route registers, so nothing says `import multipart` — a clean venv/Docker image crashes at startup without it (see /api/deploy's commit_message/files Form fields)
